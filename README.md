# tterminal-releases
The Terminal is a native command center for AI-assisted developers. Run dev servers, tests, and coding agents in one keyboard-driven workspace — see live CPU and memory per process, configure agents with toggles instead of flags, and turn browser clicks into agent-ready feedback.
